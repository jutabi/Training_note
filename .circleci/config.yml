version: 2.1
orbs:
  node: circleci/node@3.0.0

jobs:
  node:
    executor:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run:
          name: Create logs file
          command: |
            npm install
            cd bin
            node csvtologs.js
  logstash:
    docker:
      - image: logstash:7.12.0
    steps:
      - checkout
      - run:
          name: Logstash
          command: |
            cd config


workflows:
  version: 2
  build:
    jobs:
      - node
      - logstash:
          requires:
            - node